# Prompt file for ALIS software refactoring -- STAGE 0

> **Safety net and regression harness.** Before any refactoring begins, build an
> automated regression-test harness that runs the existing ALIS on the example
> fits and compares the output against the committed golden "reference" files.
> This harness is the safety net that gates every subsequent stage: no refactor
> change may land unless the harness still passes within the agreed tolerances.
> Stage 0 must not modify any code under `alis/`; it only adds test
> infrastructure (under `tests/`).

## Tasks

> Complete in order; log each in `ALIS/claude_prompts/logs/refactor_code_stage0_log.md`
> (date + description of work done).

**0.1 — Build the test-case inventory (manifest).**
- Discover every test case: any `.mod` file that has a sibling
  `<name>.mod.out.reference`, under both `examples/` and
  `context/fitting_examples/`. Discovery is by the *presence of the reference
  file*, not by filename pattern (the fit files are `fit_spectra*.mod`,
  `*_FINAL_MODEL.mod`, etc. — not `fit_<example>.mod`).
- For each test case record: example directory, fit `.mod` file, input data
  file(s) and their paired `<datadir>/reference_fits/<dataname>_fit.dat`, whether
  a `<name>.covar.reference` exists, whether the model uses randomness
  (`random` / `sim`, e.g. `DH/Q1243p307/…_converge_newstart76.mod`), whether the
  model contains **blind parameters** (see below), and its runtime → batch. The
  runtime is read directly from the `#   Running Time (hrs)` line in the
  `.mod.out.reference` (no need to re-run to measure).
- Detect blinding per model line (two mechanisms):
  - `blind=True` / `blind=T` → the value is **hidden**, rendered as a
    `------ BLIND MODEL ------` line in `.mod.out`.
  - a non-empty `blindrange=lo,hi` → the value stays **visible** but is offset by
    a fixed, `blindseed`-seeded amount (this applies even when `blind=False`).
  Ignore `blind=False` with no `blindrange`, and commented (`#`) lines. A case is
  "blind" if any active line uses either mechanism. Known blind fits:
  `DH/J1558m0031`, `DH/Q0913p072`, `DH/J1358p0349`, `DH/J0814p5029` (all
  `blind=True`; `DH/HS0105p1619` is not blind), and `examples/blind` (which
  exercises **both** modes — an O I line with `blindrange` and a Si II line with
  `blind=True`). Any blinded case is excluded from the fixed-parameter gate
  (0.2b), since feeding the blinded `.mod.out` back either re-applies the offset
  (blindrange) or has no value to evaluate (blind=True).
- Handle multiple data directories: a fit may read data from several `../data*/`
  folders (e.g. `DH/J1358p6522` uses `data/` and `data_hrdx/`; `DH_orders` uses
  `datafit_orders/`), each with its own `reference_fits/`. Resolve each input
  data file's `_fit.dat` reference relative to that file's directory, not a fixed
  `data/`.
- Classify by that runtime into the three batches of 0.3 (< 1 min fast; 1–10 min
  medium; ≥ 10 min slow). Current distribution from the references: all
  `examples/` unit fits are **fast** except `spline/…_splineContAbs` (~3.2 min)
  and `splineabs/…` (~1.7–2.8 min) which are **medium**; `helium34/tet02OriA`
  (~0.6 min) is fast, the other helium34 fits are medium; the single-object DH /
  VMP_DLA fits and even `DH_orders` (~8.3 min) are **medium**; only
  `DH/J0814p5029` (~15.9 min) is **slow**.
- Exclude `prepfit`, `lsf_spline` (see plan Q21), and note the `generate`
  example as a special case (see Q0.4 below).
- Emit the manifest in a form the harness consumes (e.g. a small module-level
  list, or a generated fixture list) so the suite is data-driven.

**0.2 — Implement the pytest regression harness (under `tests/`).**
Each non-blind test case has two test modes; blind cases run mode (a) only.

- **(a) Minimisation test.** Run `run_alis <fit>.mod` in its `model/` directory
  (data already exists — do **not** run `generate_spectra.mod`). Compare:
  - `.mod.out` to `.mod.out.reference` by **parsing numeric fields** (best-fit
    parameters and their 1σ errors, χ², DOF); **ignore volatile header lines**
    (`Generated by ALIS on …`, `Running Time (hrs)`, `Num. of Iterations`).
    **Blinded parameters** (both modes are deterministic, so `.mod.out`
    reproduces within tolerance): `blind=True` params are **hidden** as a
    `------ BLIND MODEL ------` line — skip those and compare only χ², DOF and the
    other parameters; `blindrange` params keep a **visible** fixed-offset value —
    compare them directly like any other parameter (the large offset is identical
    in both files, so it cancels in the difference). `examples/blind` exercises
    both modes in one file (O I via `blindrange`, Si II via `blind=True`).
  - the produced `<dataname>_fit.dat` model column against
    `<datadir>/reference_fits/<dataname>_fit.dat`; ignore the `-9.999999999e+09`
    sentinel rows (pixels outside `fitrange`).
  - the emitted `.covar` against `<name>.covar.reference` **only if** it exists.
- **(b) Fixed-parameter gate (option A).** Run the `.mod.out.reference` itself (it
  is a valid `.mod` input) with `chisq miniter 0` + `chisq maxiter 0`, and
  compare the produced model column to the **existing** `reference_fits/`. This is
  fast, deterministic and minimiser-independent, so it reuses the golden files
  and needs no new references. **Skip for any blinded case** (either mode) — the
  `.mod.out` value is hidden (`blind=True`) or offset (`blindrange`), so feeding
  it back cannot reproduce the true model; those rely on mode (a) only.
- **Special: the `generate` case** runs `generate_spectra.mod` and compares the
  produced data file to `data/reference_fits/<dataname>.dat` (data-generation
  test, no fit).
- Never overwrite a reference. Write generated outputs to a temp location (or
  clean them up after each test) so `.mod.out.reference`, `reference_fits/`, and
  `.covar.reference` remain pristine.

**0.3 — Batches and determinism.**
- Three pytest batches (markers), classified by per-test wall-time:
  - **fast** (< 1 min/test) — runs on every commit; the majority of tests (all
    the `examples/` unit fits **plus all fixed-parameter evaluations**, mode 0.2b,
    which are fast and deterministic even for large models).
  - **medium** (< 10 min/test) — runs nightly (e.g. the single-object DH /
    helium34 minimisation tests).
  - **slow** (on demand) — the full set, including the slowest minimisations
    (e.g. `DH_orders`, 351 spectra).
- Run **all fixed-parameter evaluations (0.2b) on every commit**, regardless of
  the case's minimisation batch.
- Handle randomised cases: `random` converges to the same best-fit but its
  `_fit.dat` varies at the ~1e-5 level run-to-run, so compare with the numeric
  tolerance (not byte-identity). Use a fixed seed / replace the random start with
  a fixed value where possible (`sim newstart` cases).

**0.4 — Tolerances.**
- **`.mod.out` best-fit parameters** (minimisation test): hard check within
  **10%** of each parameter's 1σ error; plus a softer check within **50%** of 1σ.
- **χ²:** **1%** relative for the minimisation test; **0.1%** for the
  fixed-parameter gate (0.2b).
- **`_fit.dat` model column:** **1e-4** relative (comfortably above the ~1e-5
  run-to-run `random` jitter).
- **`.covar` elements:** **1%** relative, with an absolute floor for near-zero
  elements.

**0.5 — Make the harness CI-ready, documented, and self-checked.**
- Add pytest configuration with the three markers (`fast` / `medium` / `slow`)
  and a short `tests/README` describing how to run each batch (e.g.
  `pytest -m fast`, `pytest -m "fast or medium"`, `pytest` for all). The GitHub
  Actions workflow itself is Stage 1.3.
- Before any `alis/` code is changed in Stage 1+, run the full harness a few
  independent times to confirm it is stable/deterministic and green on the
  current code (RJC's Q0.6 note).

## Skills to use for this stage

> Candidate skills (create/refine as needed under `.claude/skills/`):
- `run-example` — run a named example and verify it converges to the expected result.
- `run-tests` — run the pytest suite, filter by marker/module, report pass/fail.
- `check-fit` — parse `.mod.out` and summarise fit quality (reuse for the parser).
- `test-coverage` — identify which model functions / code paths remain untested.

## Context

- `doc/ALIS_workflow.md` — data/model/output file formats (esp. the `.mod.out`
  header, the 4-column `_fit.dat` with sentinel values, `.covar`).
- The reference-file convention and pairing rules (plan Queries Q14–Q16).
- Tolerances (plan Q11); volatile-header handling (plan Q19); fit-only + `.covar`
  testing (Prompt 9).
- Existing examples under `examples/` (25 `.mod.out.reference` fits, 23 model
  functions covered, plus the `blind`/`generate` special cases, no
  `.covar.reference`) and `context/fitting_examples/` (16 fits, 15 with
  `.covar.reference`).
- The empty `tests/` directory is the intended home for the harness.

## Queries

> Claude and RJC use this section to resolve ambiguities before/while executing.

**Q0.1 — Fixed-parameter evaluation.** The primary tolerance gate needs ALIS to
compute the model + χ² at a *given* parameter set without minimising. Is there a
supported way to do this in the current code (e.g. `chisq maxiter 0`, or reusing
the `generate`/`sim` machinery), or should the harness instead gate on the
end-to-end re-fit result only? Please advise the preferred mechanism.

**Response:** The current `.reference` files are generated from a full fit,
so they include the best-fit parameters and their associated χ² values. It is
possible to evaluate the model and χ² at a fixed parameter set by setting the
following parameters at the top parameter block of the `.mod` file:
`chisq miniter 0`
`chisq maxiter 0`
This will produce the same outputs as a normal fit, but without performing
any minimization, and the values will be different. I hesitate to recommend
this approach, since there is no minimisation being performed, and we want to
make sure the minimisation is also working. One option is to have one set of
tests that check the minimisation, and a separate set of tests that check the
fixed-parameter evaluation. The fixed-parameter evaluation can be done by
creating a new `.mod` file for each test case that copies the parameters from
the `.mod.out.reference` file and sets `miniter` and `maxiter` to 0 in the
top parameter block. This should produce (nearly) identical output models
as the `reference_fits/` files. To do an absolute comparison, we would instead
need to create an additional set of `reference_fits/` files that are evaluated
at the fixed parameter set from the starting values of the current `.mod` files.
I suspect this latter idea is preferred. In this case, we would need to create
a new set of `.mod` files that are copies of the existing `.mod` files (for example,
the original might be called `modelname.mod` and the new set would have names like
`modelname_fixedparam.mod`), but
with the `miniter` and `maxiter` to 0 in the top parameter block, and then run
those to generate a new set of `reference_fits/` files that are stored in a
separate directory (e.g. `reference_fits_fixed/`). The harness can then compare
the output of the fixed-parameter evaluation against these new reference files.
One point to note is that some of the `.mod` files may have randomised starting
parameters, so the fixed-parameter evaluation may not be deterministic. In that
case, we would need to alter the `.mod` file so that the random variable is replaced
with a fixed value for reproducibility.

**Q0.2 — `.covar` emission.** The `examples/` fit `.mod` files do not set
`out covar`, and none has a `.covar.reference`; the `context/fitting_examples/`
fits that *do* have `.covar.reference` presumably set `out covar`. Confirm the
harness should compare `.covar` **only** where a `.covar.reference` exists (and
that those `.mod` files already emit `.covar`).

**Response:** That's correct. We should only check the `.covar` output for those
test cases that have a corresponding `.covar.reference` file. The harness should
not expect or check for `.covar` output in cases where no reference exists, as
those fits are not intended to produce covariance matrices.

**Q0.3 — Harness location & framework.** Confirm the harness should live in the
existing top-level `tests/` directory and use `pytest` (a dev-only dependency,
per plan Q12).

**Response:** That is correct. The harness should be implemented in the top-level
`tests/` directory and utilize `pytest` as the testing framework. This allows for
easy integration with existing development workflows and CI/CD pipelines, while
keeping the test infrastructure organized and separate from the main codebase.

**Q0.4 — The `generate` example.** `examples/generate/` has no
`.mod.out.reference`; its purpose is to test the `generate` code path, and its
reference is `data/reference_fits/OI_SiII.dat` (the generated data, no `_fit`
suffix). Since Prompt 9 says to run only fit models, how should `generate` be
tested — as a special case that runs `generate_spectra.mod` and compares the
produced data to the reference, or left out of the automated suite for now?

**Response:** Good question. For the `generate` example, we should treat it as
a special case. The harness should run `generate_spectra.mod` and compare the
produced data file (`OI_SiII.dat`) against the reference in `data/reference_fits/`.
This ensures that the data generation functionality is also validated, even
though it does not involve fitting. We can include this as a separate test case
in the harness, clearly marked as a "data generation" test.

---

### Follow-up queries (after reviewing the Q0.1–Q0.4 responses)

I validated the mechanisms on the `powerlaw` example before asking these.

**Validation notes:**
- `chisq miniter 0` + `chisq maxiter 0` does perform a no-minimisation
  evaluation (1 iteration, Initial χ² == Bestfit χ²).
- Evaluating at the **starting** values (your option B) gave an extreme
  χ² ≈ 9.4e9 for `powerlaw` (starting `b=1.0` vs best-fit `b≈5`), and needs a
  brand-new committed `reference_fits_fixed/` golden set.
- Evaluating at the **best-fit** values instead — by running the
  `.mod.out.reference` itself (it is a valid `.mod` input) with
  `miniter=maxiter=0` — reproduced the **existing** `reference_fits/` model
  column to max rel 1.4e-7 and the reference χ² exactly, with **no new reference
  files**.
- Full re-fits are deterministic: `powerlaw` reproduced reduced χ² = 1.136408
  identically across 3 runs (7 iterations each), matching its
  `.mod.out.reference`.

**Q0.5 — Fixed-parameter gate: use "option A" (best-fit eval) instead of B?**
I recommend the fixed-parameter gate run each `.mod.out.reference` with
`miniter=maxiter=0` and compare to the **existing** `reference_fits/` (option A):
it is a sensitive forward-model regression, reuses the golden files, is
deterministic, and stays physically meaningful — unlike the starting-value
evaluation (B), which yields extreme χ² and needs a new `reference_fits_fixed/`
set. Do you agree with A? (If you still want B as an *additional* second-point
check, I will generate the `*_fixedparam.mod` files and `reference_fits_fixed/`
from the current code and replace any randomised start with a fixed value —
please confirm I should own that generation.)

**Response:** I agree with your recommendation to use option A (best-fit evaluation)
for the fixed-parameter gate. The only potential issue is that some of the models
contain blind parameters, and these are blind in the `.mod.out` and `.mod.out.reference`
files. Therefore, these cannot be used for the fixed-parameter gate. Another possibility
is to use your proposed option A for the majority of the test cases, and then for those
with blind parameters, we can ignore the fixed-parameter gate and only check the
end-to-end re-fit result.

**Q0.6 — Per-artifact tolerances.** Please confirm the tolerance for each
comparison so the harness can assert them:
- `.mod.out` best-fit parameters vs reference — within 10% of each parameter's
  1σ error (plan secondary check)?
  **Response:** This is correct. Could have a secondary softer check that ensures a parameter is within 50% of each parameter's 1σ error. 
- `.mod.out` χ² — 1% relative for the minimisation test, 0.1% for the
  fixed-parameter gate?
  **Response:** Yes, that is correct for both values.
- `_fit.dat` model column — relative tolerance (e.g. 1e-4, comfortably above the
  ~1e-5 run-to-run `random` jitter)?
  **Response:** Yes, that is fine. We should check the full harness a few independent times before changing any `alis/` code in the next stage.
- `.covar` matrix elements — tolerance (covariances are more sensitive; e.g. 1%
  relative, with an absolute floor for near-zero elements)?
  **Response:** Yes, that is a good place to start with.

**Q0.7 — Slow-test runtime budget.** A full minimisation of the large real-world
fits (esp. `DH_orders`, 351 spectra) can be very slow. For the automated suite,
should the slow tests (a) run the full minimisation only nightly / on demand, and
(b) for the very slowest, rely on the option-A fixed-parameter gate (fast,
deterministic) rather than a full re-fit? What per-test wall-time is acceptable?

**Response:** It depends how frequently we want to run the tests during code building.
I propose that we have three batches of tests:
a fast batch that runs on every commit (<1 min per test),
a medium batch that runs nightly (<10 min per test), and
a slow batch that runs all tests on demand.
The fast batch should include the majority of the tests.
We should also perform *all* fixed-parameter evaluations
on every commit, since they are fast and deterministic.

---

### Follow-up queries (after the Q0.5–Q0.7 responses / document updates)

I have updated Tasks 0.1–0.5 to reflect option A, the blind-case exclusion, the
three runtime batches (all fixed-parameter evals every commit), and the
tolerances. Two remaining points:

**Q0.8 — Blind cases in the minimisation test.** The fixed-parameter gate now
skips blind cases (agreed). But the *minimisation* test (mode 0.2a) still runs
them and compares `.mod.out` — which contains the **blinded** parameter values.
Is the blinding deterministic (fixed `blindseed`, default 0) so a re-fit
reproduces the same blinded value within tolerance? If so, the comparison works
as-is. If not, should the harness skip the blinded parameter(s) in the `.mod.out`
comparison and check only χ², DOF, and the non-blind parameters? Also: please
confirm my blind-detection heuristic (`run blind True`, or an active
`blind=True`/`blind=T` model line; ignoring `blind=False` and commented lines) is
sufficient, or whether you can mark blind cases explicitly.

**Response:** Your blind detection heuristic is sufficient. The blinding in the current
test-suite is deterministic, so a re-fit will reproduce the same blinded values within
the specified tolerances. If a parameter is blinded, the line of the `.mod.out.reference`
file will look something like the following:
`       ------ BLIND MODEL ------`
and there's no parameter values that can be tested. The harness should skip
the blinded parameter(s) in the `.mod.out` comparison and check only χ², DOF,
and the non-blind parameters. Note there are two ways of blinding in ALIS:
- if `blind=True`, the blinded value is hidden from view (as described above). Assuming `blindrange` is NOT set, no random seed is used, and there will be no fixed blind offset.
- if `blindrange` is set, then the value is NOT hidden from view. Instead, a random offset is applied (based on the `blindseed` and `blindrange`), and the value is still visible in the `.mod.out`. Currently, there are not examples that use this functionality. If it would be helpful, then perhaps we can add this as an additional test for one of the quicker examples in the `examples/` directory.

**Q0.9 — Medium vs slow classification.** To assign the `context/fitting_examples/`
fits to the medium (<10 min) vs slow (on-demand) batches I need their runtimes.
Shall I measure them during 0.1 by running each once (note: `DH_orders`, 351
spectra, may take a long time even for a single run), or can you advise the
split? Proposed default until measured: single-object DH and helium34
minimisations → **medium**; `DH_orders` → **slow**; all `examples/` unit fits and
*every* fixed-parameter evaluation → **fast**.

**Response:** The runtimes are all provided in the `.mod.out.reference` files.
For example, the commented out line near the top of the file reads something like:
`#   Running Time (hrs)  = 0.003519`, where the value is in hours. You can use this
to classify the tests into the three batches.

---

### Follow-up query (after the Q0.8–Q0.9 responses / document updates)

I have folded both answers into the document: 0.1 now reads the batch runtime from
the reference `Running Time (hrs)` and records the resulting fast/medium/slow
split (all `examples/` fast except `spline/…_splineContAbs` and `splineabs`
which are medium; `DH_orders` ≈ 8.3 min is medium; only `DH/J0814p5029` ≈ 15.9 min
is slow); and 0.2a now skips `------ BLIND MODEL ------` lines and compares only
χ², DOF, and non-blind parameters.

**Q0.10 — Add a `blindrange` test example?** Your Q0.8 note describes a second
blinding mode (`blindrange`: the value stays visible but with a fixed, seeded
offset) that no current example exercises. Would you like me to add a small
`blindrange` example (e.g. a variant of a fast `examples/` case) so the harness
covers that mode? Because the offset is fixed/seeded, its `.mod.out` would be
deterministic and directly comparable (no need to skip the parameter). If yes,
I'll create it and generate its references from the current code during Stage 0.

**Response:** I have created this example: `examples/blind/` with a small
`.mod` file that uses `blindrange` and a fixed seed. The harness should include
this test case, and it will be deterministic, so the `.mod.out` can be compared
directly against its reference without skipping any parameters.

---

### Follow-up query (after the Q0.10 response / document updates)

I inspected `examples/blind/`. It actually exercises **both** blinding modes in
one file: the O I line uses `blindrange` (visible offset value `-616947.11` in
`.mod.out`) and the Si II line uses `blind=True` (hidden as `------ BLIND MODEL
------`). I have updated the document so 0.2a compares `blindrange` params
directly but skips `blind=True` lines, and refined the blind-detection rule to
treat a non-empty `blindrange=` as a blinding mechanism even when `blind=False`.

**Q0.11 — Fixed-parameter gate for the blind example.** Your Q0.10 note confirms
the *minimisation* test compares `examples/blind`'s `.mod.out` directly. I have
**excluded it from the fixed-parameter gate (0.2b)**, because running its
`.mod.out.reference` back through ALIS would re-apply the `blindrange` offset to
the O I value (double offset) and the Si II line has no value to evaluate. Please
confirm this exclusion is what you intended (i.e. `examples/blind` is a
minimisation-only test case).

With that confirmed, I believe the Stage 0 specification is complete and ready to
implement (0.1 → 0.5).

## Prompts

1. Read this doc, check my responses to the queries, and ask more queries if needed.

2. Read this doc, check my responses to the queries. Some updates will be needed to this document. Then, ask more queries if needed.

3. Read this doc, check my responses to the queries. Some updates will be needed to this document. Then, ask more queries if needed.

4. Read this doc, check my responses to the queries. Some updates will be needed to this document. Then, ask more queries if needed.

