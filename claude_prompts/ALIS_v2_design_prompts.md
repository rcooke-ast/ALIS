# Improvements to the code structure of ALIS 

## Goals

- Review the `ALIS/claude_prompts/context.md` file (particularly the `Context` Section) to have a greater understanding of the goals of the new version of ALIS.

## Claude

### Skills

Consider using the skills in `ALIS/.claude/skills/` for any task that would benefit.

### Context

When developing new code, use the context provided in `ALIS/context/`. This includes:

- The most important context is provided by example model fits themselves and their outputs. These are provided in `ALIS/context/fitting_examples/`. There are multiple `context.md` files in many of the subfolders of `fitting_examples/` that describe the context of the example fits. Read those to obtain a greater understanding of the context of the example fits. The new version of ALIS should successfully run on these examples and produce similar outputs.
- Information in any of the latex files provided in the `ALIS/context/publications_using_alis/` folder.
- An example of several scripts and files that demonstrate a voigt profile computation on a GPU in the `ALIS/context/voigt_gpu/` folder.
- An example of latex documentation provided by an independent Voigt profile fitting package is provided in `ALIS/context/VPFIT_doc/`.

## Coding

Here are guidelines for coding: 

- Use Python where possible
- The main files used by the code are in `ALIS/alis/`
- Add inline comments to explain the effort
- Reuse existing code when possible
- Place import statements at the top of the file.
- Include a description of inputs/outputs in the doc string of all methods
- Limit lines of code to 80 characters per line
- Add "Generated by RJC and Claude" in the doc string of all methods you create

## Workflow doc

To design a user friendly new version of ALIS, it will be necessary for Claude to understand well the workflow of preparing, running, and analysing an ALIS fit.  To keep a record of that context, we will generate a document called `ALIS/doc/ALIS_workflow.md`.  This document will be used to guide the development of the new version of ALIS.

1. Let's begin by examining the latex document in `ALIS/doc/`. Note that this documentation is out of date. You will need to refer to the code in order to obtain more up-to-date understanding of the code. Do the following:
   - Read the latex file in `ALIS/doc/`, with particular focus on `ALIS/doc/tex_files/description.tex`
   - Identify the key components of performing an ALIS fit
   - Generate a draft of the `ALIS_workflow.md` document
   - Add a version number to the document (0.1)
   - Add a date to the document (today's date)
   - Add a author to the document (RJC and Claude)
   - Log your work in the Logs section below

2. Now let's run ALIS on a test dataset.  Use the dataset in `ALIS/examples/metal_line_abs/model/fit_spectra.mod` and run this example in that directory. Do the following:

- Be aware this is one of the simplest example fits in ALIS
- Execute the following command: `run_alis fit_spectra.mod`
- Let the fit run to completion
- Note the output folder and output files. The output file will be called `ALIS/examples/metal_line_abs/model/fit_spectra.mod.out`.
- Note that these data are synthetic and the fit should be very good. The synthetic data were generated with `ALIS/examples/metal_line_abs/model/generate_spectra.mod`
- Examine the input and output data files to understand the quality of the fit, 
  - The input data file is `ALIS/examples/metal_line_abs/data/OI_SiII.dat`
  - The output data file is `ALIS/examples/metal_line_abs/data/OI_SiII_fit.dat`
  - Compare the two files to understand the quality of the fit, and what information is in each of the columns of data.
- Examine the saved pdf file showing the quality of the fits (`fit_spectra.pdf`)
- Update the `ALIS_workflow.md` document with the new information
- Update the version number and include the author and date
- Log your work in the Logs section below

#### Clarifications

Questions raised while drafting the workflow document:

- The LaTeX files are incomplete (e.g., `fitting.tex` is a placeholder; several `alfunc_` files are undocumented). The source code and fitting example context files are the authoritative reference.
  **Response:** Yes, this is correct.
- Two analysis workflows are used in practice (`model/` and `fitcont_model/`). The LaTeX docs only cover `model/`.
  **Response:** The distinction between the directory `model/` or `fitcont_model/` is irrelevant. It's just a name of a folder. ALIS does not care about the name of the folder. There is absolutely no difference what-so-ever between folders named `model/` and `fitcont_model/`. You should assume these names are the same and interchangeable.
- Convergence testing via external Monte Carlo (running many instances with random starting parameters) is not in the LaTeX docs; it was inferred from the fitting examples.
  **Response:** That is correct. There is no documentation about the Monte Carlo procedure at this stage. The authoritative reference for the Monte Carlo procedure is the source code.


## Prompts

1. Read this doc.  Perform Task 1 under Workflow doc
2. Read this doc, as I have updated it, and responded to your clarification questions. I have also updated the `ALIS_workflow.md` document to correct some confusions and mistakes. Once you have checked these updates, please perform Task 2 under Workflow doc.
3. Please perform the fit again from Task 2, there was a bug in the input file. Then examine the output files and the pdf file.  Update the `ALIS_workflow.md` document with your findings, and log your work in the Logs section.

## Logging

Create a file called `ALIS/logs/ALIS_v2_design_logs.md` to record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

Simple description of the work and what you learned

### <Date> (Short summary of the work)

Simple description of the work and what you learned

...

